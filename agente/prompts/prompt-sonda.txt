PASO 0 (obligatorio, antes que cualquier otra cosa): seleccioná el navegador con
deviceId {{DEVICE_ID}} usando la herramienta select_browser. Puede haber varios
Chrome conectados a la cuenta y este es el de esta maquina. Si ese deviceId no
esta disponible, devolve el JSON de error con motivo "browser_no_disponible".

Tarea de DIAGNOSTICO y de SOLO LECTURA.

NO leas el contenido de ningun chat. NO abras ningun chat. NO toques el campo de
escritura. NO envies nada. Lo unico que hace falta saber es si desde aca se
puede llegar a la pagina, y eso se responde sin mirar ninguna conversacion.

Pasos:

1. Abri una pestania en https://web.whatsapp.com y espera a que cargue.

2. Fijate si la sesion esta iniciada, es decir si se ve la lista de chats, o si
   la pagina pide escanear un codigo QR.

3. Conta cuantas conversaciones se ven en la lista. Solo el numero: no anotes
   nombres, ni telefonos, ni nada de lo que digan.

4. Devolve UNICAMENTE este objeto JSON, sin texto antes ni despues, sin
   backticks:

{ "status": "ok", "sesion_iniciada": true, "chats_visibles": 0 }

Si algo falla:

{ "status": "error", "motivo": "sesion_no_iniciada|browser_no_disponible|sin_permiso|otro", "detalle": "..." }

Si el navegador responde que falta permiso para el sitio, usa motivo
"sin_permiso" y copia el mensaje exacto del navegador en detalle.

RECORDATORIO FINAL: la salida se parsea automaticamente, asi que tu respuesta
debe ser el objeto JSON y nada mas: sin preambulo, sin backticks. Si no pudiste
completar la tarea, el JSON de error ES la respuesta correcta.
